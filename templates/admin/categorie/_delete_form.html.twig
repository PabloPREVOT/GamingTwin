    <form method="post" action="{{ path('admin_categorie_delete', {'id': categorie.id}) }}" onsubmit="return confirm('Vous êtes bien sur de vouloir supprimer {{ categorie.nom }} ?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie.id) }}">
        <button class="btn btn-danger">Supprimer</button>
    </form>
</div>
