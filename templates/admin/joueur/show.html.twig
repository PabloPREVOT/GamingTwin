{% extends 'base.html.twig' %}

{% block title %}Joueur{% endblock %}

{% block body %}
    <h1>Joueur : {{ joueur.pseudo }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ joueur.id }}</td>
            </tr>
            <tr>
                <th>Pseudo</th>
                <td>{{ joueur.pseudo }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ joueur.roles ? joueur.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ joueur.password }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ joueur.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ joueur.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ joueur.email }}</td>
            </tr>
            <tr>
                <th>Discord_id</th>
                <td>{{ joueur.discordId }}</td>
            </tr>
            <tr>
                <th>Steam_id</th>
                <td>{{ joueur.steamId }}</td>
            </tr>
            <tr>
                <th>Tryhard_meter</th>
                <td>{{ joueur.tryhardMeter }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ joueur.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_joueur_index') }}">Retourner à la liste des joueurs</a>

    <a href="{{ path('admin_joueur_edit', {'id': joueur.id}) }}">Modifier</a>

    {{ include('admin/joueur/_delete_form.html.twig') }}
{% endblock %}
